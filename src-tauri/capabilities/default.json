{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "harness-remote",
  "description": "The remote Harness iframe is intentionally denied direct Tauri IPC. Its native integrations are mediated by the local desktop shell through origin-checked postMessage bridges. The service port is user-configurable and may advance to the next free loopback port, so the URL pattern must cover loopback ports without granting any system permission.",
  "local": false,
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  },
  "permissions": []
}
